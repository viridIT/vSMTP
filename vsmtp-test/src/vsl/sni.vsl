#{
    rcpt: [
        rule "check server_name with sni" || {
            if ctx.server_name == "second.testserver.com" {
                sys::accept()
            } else {
                sys::deny()
            }
        }
    ]
}
