#{
    connect: [
        rule "test_socket" || {
            print(server.client_addr.to_string());
            print(server.client_addr.to_debug());
            if server.client_addr.to_string() is "0.0.0.0:0"
            && server.client_addr.to_debug() is "0.0.0.0:0"

            && server.client_addr is "0.0.0.0"
            && server.client_addr == "0.0.0.0"
            && server.client_addr not "127.0.0.1"
            && server.client_addr != "127.0.0.1"

            {
                vsl::accept()
            } else {
                vsl::deny()
            }
        }
    ]
}