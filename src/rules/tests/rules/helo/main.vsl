object fqdn "viridit" "viridit.com";
object file:fqdn "domains" "./src/rules/tests/rules/helo/helo.txt";
object regex "com" "^[a-z0-9.]+.com$";

#{
  helo: [
    rule "test_fqdn" || {
      if server.helo is viridit && viridit is server.helo {
        vsl::next()
      } else {
        vsl::deny()
      }
    },

    rule "test_file" || {
      if server.helo in domains {
        vsl::next()
      } else {
        vsl::deny()
      }
    },

    rule "test_regex" || {
      if server.helo is com {
        vsl::next()
      } else {
        vsl::deny()
      }
    },
  ]
}