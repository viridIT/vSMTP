#{
    postq: [
        rule "get hostname" || {
            print("running get hostname: ");
            print(vsl::hostname());
            vsl::accept()
        }
    ],

    delivery: [
        rule "find users" || {
            if !vsl::user_exist("john")
            && vsl::user_exist("root")
            {
                vsl::accept()
            } else {
                vsl::deny()
            }
        }
    ]
}
