#{
    preq: [
        action "action + set in quarantine" || {
            sys::quarantine(srv, ctx, "tests/generated/quarantine1");
        },

        rule "trailing" || sys::accept(),
    ],

    postq: [
        rule "set in second quarantine + deny" || {
            sys::quarantine(srv, ctx, "tests/generated/quarantine2")
        },

        rule "trailing" || sys::accept(),
    ]
}
