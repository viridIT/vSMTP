import "mod/antivirus" as antivirus;

run_rules!(#{
    preq: [
        action "antivirus" || {
            if antivirus::has_virus(services, ctx) {
                // quarantine and log and blacklist ?
            }
        }
    ]
})
